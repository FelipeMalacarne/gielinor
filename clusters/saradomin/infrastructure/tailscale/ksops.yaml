apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: tailscale-oauth-secrets
  namespace: tailscale
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - secrets.yaml
