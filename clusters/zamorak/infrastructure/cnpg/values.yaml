# CloudNativePG operator values.
# Keep this lean for a single-node Oracle/k3s cluster.

config:
  clusterWide: true

replicaCount: 1

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

monitoring:
  podMonitorEnabled: false
