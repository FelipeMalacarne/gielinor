apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - messaging-topology-operator-v1.19.3.yaml

patches:
  - target:
      version: v1
      kind: Namespace
      name: rabbitmq-system
    patch: |-
      $patch: delete
      apiVersion: v1
      kind: Namespace
      metadata:
        name: rabbitmq-system
