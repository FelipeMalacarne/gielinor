apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: postgres-main-vaultwarden
spec:
  cluster:
    name: postgres-main

  name: vaultwarden
  owner: vaultwarden

  databaseReclaimPolicy: retain
