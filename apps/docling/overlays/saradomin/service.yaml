apiVersion: v1
kind: Service
metadata:
  name: docling-serve
  namespace: ai
  labels:
    app.kubernetes.io/name: docling-serve
    app.kubernetes.io/part-of: concursos-pipeline
spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: docling-serve

  ports:
    - name: http
      port: 5001
      targetPort: http
      protocol: TCP
